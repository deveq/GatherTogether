<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.soldemom.navermapactivity">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MemberApproveActivity" />
        <activity android:name=".FcmTestActivity" />
        <activity android:name=".TestActivity3" />
        <activity android:name=".TestActivity2" />
        <activity android:name=".TestActivity" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".MyGatheringListActivity" />
        <activity android:name=".CreateGatheringActivity" />
        <activity android:name=".LoginActivity"/>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="@string/naver_map_client_id" />

        <activity android:name=".MainActivity" />
    </application>

</manifest>